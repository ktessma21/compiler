## Questions
    1. for normal number and even a pointer if we reserver the last bit for marker how can we store the number then, specifically big numbers
    2. Does the first is (@main (@main)) the second has to be main ? ... it shouldn't necessarily has to be main. 
    
    

